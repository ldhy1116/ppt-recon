# 大模型配置示例（OpenAI 兼容接口）
# 复制本文件为 .env 并按需修改；.env 已被 .gitignore 排除，不会入库。
# 不配置时：model_config.py 内置本地 Ollama 默认值；模型不可用时回退 0-token 关键词模式。

# 本地 Ollama（默认，无需真实密钥，占位值 ollama 仅为满足接口约定）
OPENAI_BASE_URL=http://localhost:11434/v1
OPENAI_API_KEY=ollama
OPENAI_MODEL=qwen2.5:3b

# 如需切换远程 OpenAI 兼容服务，改为真实端点与密钥（切勿把真实密钥提交到仓库）：
# OPENAI_BASE_URL=https://api.example.com/v1
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
# OPENAI_MODEL=gpt-4o-mini

# 也可通过系统环境变量或 nanobot 等 Agent Runtime 注入，优先级高于本文件。
