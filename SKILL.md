---
name: "ppt-reorganize"
description: "按汇报目的重组PPT页面顺序（自然语言入口 chat.py；两级编号树切章、分层目的投票、章内目的精排）。当用户要求为特定演讲场景重排或重组PPT时调用。"
---

# PPT 智能重组

按自然语言描述的汇报目的，重组 PPT 页面顺序，使内容编排更适合特定演讲场景。

## 何时调用

当用户表达以下意图之一时调用本 Skill：

- "把这份 PPT 按投资人路演重组"
- "帮我把 test2.pptx 面向课堂教学的场景重新组织一下"
- "这份 PPT 适合课堂教学的顺序是什么"
- "按招生宣讲的目的重组 PPT"
- 任何涉及调整 PPT 页面顺序以适应特定汇报/演讲场景的请求

## 主入口：chat.py（自然语言，推荐）

```powershell
cd <仓库根目录>
$env:PYTHONIOENCODING='utf-8'   # 防中文乱码（每个终端一次）
python chat.py "帮我把 test3.pptx 面向招生宣讲的场景重新组织一下，并删去旧目录增加新目录，存到test3_intro"
```

- PPT 放在 `ppts\` 下，自然语言里只写文件名
- 意图解析：`reorganize`（重组）/ `reorder`（单页重排）/ `toc`（目录）/ `analyze`（分析）/
  `--parse` 只解析不执行（调试）
- 输出默认到 `data\`，`存到xxx` 指定产物名（可省略 .pptx）
- 删旧目录/增新目录：`删去旧目录增加新目录`

## 前置条件

### 1. 环境与依赖

- Python 3.10+；依赖见 `requirements.txt`（`pip install python-pptx openai`）

### 2. 大模型配置（由 model_config.py 统一加载，不写死于代码）

| 环境变量 | 默认值（Ollama 本地） |
|---------|------|
| `OPENAI_BASE_URL` | `http://localhost:11434/v1` |
| `OPENAI_API_KEY` | `ollama` |
| `OPENAI_MODEL` | `qwen2.5:3b` |

配置优先级：已设环境变量 > .env 文件 > 内置默认值。
nanobot 等 Agent 可直接注入环境变量切换模型，项目内部无需感知。
未配置/模型不可用时自动回退关键词模式（0 token）。

### 3. 测试 PPT（ppts\，脱敏合成用例，随仓库提交）

| 文件 | 页数 | 结构特征 |
|------|------|---------|
| test1.pptx | 18 | 研究报告、无分标题：标题归组切章 + 全页 LLM 排序 |
| test2.pptx | 57 | 课件、单层「第N节」分标题：结构切章 + 章投票 + 章内精排 |
| test3.pptx | 60 | 招生宣讲、两级编号：主板块编号章 01-10 + 剥离的 02 招生/03 问答板块 |

> 用户真实 PPT 放入 `ppts\` 即可（已被 .gitignore 排除，不会入库）。

## 目的识别（12 种，chat.py PURPOSE_RULES 有序关键词）

| 用户说法举例 | 归一化目的 |
|------------|----------|
| 科普/普及/大众科学 | 科普宣讲 |
| 答辩/毕业论文/开题/科研报告 | 学术答辩 |
| 年终/年度总结/述职/复盘 | 工作总结 |
| 项目/课题/结题/立项 | 项目汇报 |
| 发布会/新品/产品发布 | 产品发布 |
| 投资人/融资/路演/商业计划 | 投资人路演 |
| 招生/宣讲会/招生简章 | 招生综合宣讲 |
| 培训/讲座/研修 | 培训讲座 |
| 教学/授课/课堂/讲课 | 课堂教学 |
| 技术分享/技术汇报 | 技术汇报 |
| 产品评审/评审会 | 产品评审 |
| 工作汇报/综合汇报 | 综合汇报 |

自然语言里出现明确目的词时以规则命中为准（不被模型联想带偏）。

## 排序逻辑（pptx_reorganize.py resolve_order）

```
粗排（大稿跳过全页 LLM，关键词序兜底）
→ 章节检定升级链：①结构分标题切章(0 token) → ②标题归组 → ③LLM 聚类 → ④锚定兜底
→ 两级编号树（split_top_sections，0 token）：
    编号章内「连续≥2页同号且≠章号」的编号段（含挂靠无号页）剥离为顶层板块
    （如 test3：编号章 01-10，02 招生/03 问答板块剥离后与章平级）
→ 分层目的投票：顶层板块间投票（3 轮 Borda）→ 主板块内编号章间投票
    （5 轮 Borda，候选多以压低跨运行波动）；温度首轮 0.1、其余 0.15
    板块以分标题展示名+自身页关键词参与；失败/未用LLM→编号升序 板块 01→02/03）
    主板块开篇组（含板块分标题页）恒定板块首位，不参与投票
→ 章内精排：每章 LLM 按目的重排（2-24 页窗口，注入 12 目的叙事准则，
    全部未命中→必须返回原序）；LLM 失败/无效→原序兜底；3b 漏排的页
    按原稿邻接位置插回（前邻之后/后邻之前），不甩章末
→ 证据精排（编号归位；LLM 已排叙事时跳过阶段重排）→ 特殊页锚定
```

- 结构信号（分标题/编号）优先且 0 token，保证章节成块、结果可复现
- 章间与章内均由目的驱动：同一份 PPT 不同目的产出不同章序/章内序
- 特殊页（封面/目录/总结/参考/致谢）恒锚定首尾；分标题恒领章首

### CLI 直调（高级，chat.py 的底层通道）

```powershell
python pptx_reorganize_cli.py reorganize "ppts\test3.pptx" --purpose "招生综合宣讲" -o "data\test3_intro.pptx" --smart --use-llm --trim --yes --force
python pptx_reorganize_cli.py analyze "ppts\test3.pptx"   # 先看结构
```

解读 trace：`induce_mode`（检定路径）、`top_sections`（两级板块）、
`induced_chapters`（最终章列表）、`within_chapter_calls/ok`（章内精调）。

## 输出约定

产物默认输出到 `data\`（目录已留空占位，按需生成）；
命名建议 `<ppt名>_<目的缩写>.pptx`（如 `test3_intro.pptx`）。

## 限制

- 3b 本地模型对模糊指令的理解有限：目的词请尽量明确（说「答辩」而不是「正式一点」）
- 单次端到端约 5-7 分钟/目的（3b，多轮投票+章内精排），多目的需串行
- 非中文 PPT 效果可能下降（关键词和种子词为中文）
- 纯图片型/无文本 PPT 无法提取标题，退化为锚定兜底

## 相关文件

| 文件 | 用途 |
|------|------|
| `chat.py` | 自然语言入口（意图解析→调用核心库） |
| `pptx_reorganize_cli.py` | CLI 入口（参数直调） |
| `pptx_reorganize.py` | 核心库（检定/两级树/投票/精排） |
| `model_config.py` | 模型配置加载（.env→环境变量→默认值） |
| `ppts\` | 测试 PPT 输入目录 |
| `data\` | 输出产物目录 |
| `tests\test_reorganize.py` | 单元测试（107 项） |
| `run_examples.ps1` | 一键示例脚本 |
| `README.md` | 使用文档 |
